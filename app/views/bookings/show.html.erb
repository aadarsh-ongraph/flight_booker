<h2>Booking Details:</h2>
<p>Flight: <%= @booking.flight.departure_airport.code %> to <%= @booking.flight.arrival_airport.code %></p>
<p>Date: <%= @booking.flight.start_datetime.strftime("%B %e, %Y") %></p>
<p>Passenger Count: <%= @booking.passengers.count %></p>

<h2>Passenger Information:</h2>
<ul>
  <% @booking.passengers.each do |passenger| %>
    <li><strong>Name:</strong> <%= passenger.name %>, <strong>Email:</strong> <%= passenger.email %></li>
  <% end %>
</ul>
